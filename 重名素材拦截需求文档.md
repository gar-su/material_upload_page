# 需求文档：素材重名拦截
## 版本
| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-09-22 | - | 初稿 |
## 背景
素材库内素材以「素材名」作为业务标识，上游各生产路径均按结构化规则生成名称（如 `968_LIHE_EC_ID_LM017_260509_F_F_NU.mp4`），名称本身已编码项目、日期与序号，天然具备全局唯一性。但当前两条入库路径（上传页手动上传、AI素材库同步）均不校验重名，同一条素材被重复上传或重复同步时会生成同名卡片，导致投放侧取数重复、素材归属与状态无法对应。

本需求确立「素材名在素材库内全库唯一」这一约束，并统一两个入库入口的判定口径与拦截行为。
## 目标
- 素材名在素材库内全局唯一，不限文件夹。
- 上传页与 AI素材库同步共用同一套判定口径与后端查名接口。
- 重名在「加入列表 / 生成名称」阶段即暴露并给出改名出路，不静默丢弃、不覆盖、不自动改名。
## 范围
- **涉及页面**：NS 后台 - 上传素材弹窗、NS 后台 - 素材库 - AI素材库 tab
- **涉及系统**：NS 后台、NS 批量素材库素材桶、素材名查重接口
- **涉及功能**：同名判定口径、加入列表时即时校验、名称就地编辑、上传拦截、同步命名冲突拦截
---
## 功能详述
### F1. 判定口径
素材名在素材库内全局唯一，不限文件夹。
| 维度 | 规则 |
|------|------|
| 比较对象 | 全库已有素材名 + 本次批次内其他素材名 |
| 名称构成 | 主名 + 扩展名，扩展名参与比较 |
| 规范化 | 去除首尾空格后忽略大小写 |
| 空主名 | 视为不可上传，与同名一并拦截 |
由此，`968_XXX.mp4` 与 `968_XXX.mov` 视为两条不同素材可共存，`968_XXX.mp4` 与 ` 968_xxx.MP4 ` 视为同名。批内查重不可省略：同一批选中来自不同目录的同名文件时，只查全库会漏判。
### F2. 入口一：上传页加入列表时即时校验
| 维度 | 规则 |
|------|------|
| 触发时机 | 文件加入列表时即查全库并判定，不等点击「完成」 |
| 冲突展示 | 同名文件仍加入列表并就地标红，提示「与素材库已有素材同名」，不做静默丢弃 |
| 名称编辑 | 「名称」列主名可编辑、扩展名固定展示不可编辑，防止改名后名实不符 |
| 实时重判 | 修改主名后实时重新判定，冲突解除即清除标红 |
| 上传拦截 | 点击「完成」先校验同名，再校验短剧绑定，存在同名或空主名时弹窗拦截 |
拦截弹窗文案：
```
有 N 个素材与素材库已有素材同名，请重命名后再上传
```
（N 为冲突素材数量，突出显示。）
弹窗仅提供「知道了」关闭入口，不提供放行选项。同名素材禁止上传，与「未绑定短剧」可无视风险放行的语义相反，两者不可合并为同一弹窗。
### F3. 入口二：AI素材库同步命名冲突
| 维度 | 规则 |
|------|------|
| 触发时机 | 「同步素材」弹窗内，随首个素材名称输入实时校验 |
| 查重对象 | 按选中顺序递增生成的每个「同步后名称 + 源文件扩展名」，与全库已有素材名比对 |
| 冲突展示 | 命名预览只展示首个素材一行，其余名称的冲突在预览区下方汇总列出 |
| 同步拦截 | 存在冲突时点击「确定同步」被阻断，提示调整首个素材名称 |
| 出路 | 修改首个素材名称，冲突消除后可正常同步 |
| 校验顺序 | 名称冲突先于目标文件夹校验，与弹窗内字段顺序一致 |
不跳过、不覆盖、不自动改名：同步是批量动作，静默跳过会让用户误以为全部成功。
### F4. 边界与例外
- 扩展名参与比较，同一素材的 mp4 / mov 双封装版本视为两条不同素材，可共存。
- 大小写、首尾空格差异不构成不同素材。
- 仅做规范化后的精确比对，不做模糊匹配与相似度判断。
- 素材库内历史已存在的同名素材不在本需求清理范围，本需求只约束新增。
### F5. 后端查名接口
上传页「加入即查全库」若逐个文件调用，批量拖入 50 个文件即产生 50 次请求，因此按批设计。
| 维度 | 规则 |
|------|------|
| 接口 | `POST /materials/check-names` |
| 请求体 | `{ names: ['968_XXX.mp4', ...] }`，提交原始文件名 |
| 响应 | `{ conflicts: ['968_XXX.mp4', ...] }` |
| 比对口径 | 后端按 F1 口径统一比对，前端不承担规范化职责 |
| 批大小 | 单批名称数量上限待确认 Q2 |
| 竞态 | 校验与写入之间存在时间窗口，最终唯一性由入库侧兜底，见 Q1 |
---
## 交互流程
### 上传页
```
点击拖拽区 / 文件夹上传 / 上传素材 → 逐个加入文件行
  → 即时按「全库已有素材名 + 本批内其他行」判定
      ├─ 无冲突 → 正常展示
      └─ 同名或空主名 → 行内标红并提示原因，仍保留在列表中
  → 用户就地修改主名（扩展名固定不可改）→ 实时重新判定
  → 点击「完成」→ 先校验同名，再校验短剧绑定
      ├─ 存在同名或空主名 → 弹出「有 N 个素材与素材库已有素材同名，请重命名后再上传」
      │     └─ 知道了 → 关闭弹窗，返回上传页改名（无放行入口）
      └─ 无同名 → 进入原有短剧绑定校验
```
### AI素材库同步
```
勾选素材 → 点「同步」→ 拉起「同步素材」弹窗
  → 输入首个素材名称 → 系统识别数字段递增，实时刷新命名预览
  → 生成名称与全库已有素材同名 → 预览区下方提示冲突
  → 点「确定同步」→ 被阻断，提示调整首个素材名称
  → 调整名称消除冲突 → 点「确定同步」→ 正常同步
```
---
## 验收标准
1. 素材名判定为全库唯一，不限文件夹。
2. 同名判定忽略大小写与首尾空格，扩展名参与比较：`968_XXX.mp4` 与 `968_XXX.mov` 不判为同名。
3. 上传页文件加入列表时即与全库已有素材名比对，同名的行标红并提示「与素材库已有素材同名」，且仍保留在列表中不被丢弃。
4. 本次列表内两行同名时同样标红。
5. 上传页名称列主名可编辑、扩展名固定不可编辑；改名后实时重新判定，冲突解除即清除标红。
6. 存在同名或空主名时点「完成」，弹出同名拦截弹窗并显示冲突数量，弹窗不提供放行入口。
7. 冲突清除后点「完成」回到原有的未绑定短剧拦截逻辑。
8. AI素材库同步弹窗内，生成名称与全库已有素材同名时提示冲突，且「确定同步」被阻断。
9. 调整首个素材名称消除冲突后，「确定同步」可正常执行。
10. 任何入口都不对同名素材做静默跳过、覆盖或自动改名。
---
## 待确认项
| 编号 | 事项 | 影响 | 建议方案 |
|------|------|------|------|
| Q1 | 入库侧是否建唯一索引兜底 | 决定并发写入时能否保证唯一 | 建议建，查名接口只做前置提示 |
| Q2 | 单批查名的名称数量上限 | 决定前端分批策略 | 待定，需结合上传单次上限 |
| Q3 | 查名接口的鉴权与可见范围 | 决定越权探测风险 | 建议仅校验名称存在性，不返回素材详情 |
---
## Mock 数据说明
当前原型 `index.html` 中重名相关数据为前端写死的 mock 数据，接入真实接口时替换即可，交互逻辑无需改动。
| 数据 | 位置 | 说明 |
|------|------|------|
| 全库已有素材名 `MOCK_LIBRARY_NAMES` | `<script>` 顶部 | 字符串数组，含扩展名，查重数据源 |
| 模拟加入列表的文件 `MOCK_UPLOAD_FILES` | `<script>` 顶部 | 点击拖拽区 / 上传按钮逐个追加，前两条与全库同名、第三条仅扩展名不同 |
| 判定与展示 | `isDupName()` / `collectDupRows()` / `refreshDupMarks()` / `syncConflicts()` | 判定口径的唯一实现处 |
原型中两个入口的演示路径：
- 上传页：打开「上传素材」弹窗，点拖拽区或「上传素材」按钮追加文件行，第 1、2、4 行与全库同名或与本批内重名，行内标红；点「完成」弹出拦截弹窗。
- AI素材库：切到「AI素材库」tab，勾选第 2 张卡片（`895_QIKEYOU_EC_TR_P569_260909_FF_NU_Q.mp4`），点「同步」，弹窗内默认预填的名称即与全库同名，预览区下方直接出现冲突提示，点「确定同步」被阻断。
`MOCK_LIBRARY_NAMES` 中的 `895_QIKEYOU_EC_TR_P569_260909_FF_NU_Q.mp4` 与 AI素材库第 2 张卡片同名，是刻意构造的冲突样本——该素材此前已入过批量素材库，AI 平台侧又产出了一条同名素材（AI素材库幂等键为平台素材 ID 而非文件名，同名不同 ID 可并存），正是本需求要拦的场景。缺少该样本时，同步侧冲突只能靠手动输入特定名称才能触发。
---
## 不涉及（范围外）
- 历史存量同名素材的清理与合并，本需求只约束新增。
- 同名素材的自动重命名（如追加 `_1` 后缀），会破坏结构化命名规范。
- 素材删除、移动、改名等库内管理能力。
- 上传进度展示与真实文件读取，原型阶段以 mock 数据模拟加入列表。
