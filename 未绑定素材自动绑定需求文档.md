# 需求文档：未绑定素材自动绑定定时任务
## 版本
| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-25 | - | 初稿 |
## 背景
上传素材流程中，素材的「短剧信息」绑定为一条必要元数据。当前前端上传页已支持手动绑定 + 未绑定拦截，并保留「无视风险继续上传」的放行口，真实系统因此会持续产生「已上传但短剧绑定为空」的素材。

后端「文件名解析 → 短剧推导」能力已实现，素材表含有上传时间、绑定字段与素材状态。本需求在真实后端新增一个定时任务，周期性调用该推导能力，把近期上传的未绑定素材自动补绑，减少人工补绑成本，并对强制放行形成自动兜底闭环。
## 目标
任务每 6 小时运行一次，捞取最近一周内上传、且已过 24h 手动保护期的未绑定素材，逐条调用「文件名 → 短剧」推导服务自动补绑；历史存量（上传时间 > 7 天）不在本轮范围。前端 upload 页零改动。
## 范围
- **涉及页面**：无（前端上传页零改动）
- **涉及系统**：真实后端定时任务（自动化素材项目侧），依赖素材表与已有推导服务
- **涉及功能**：未绑定素材查询、推导调用、绑定写回、每轮汇总日志
---
## 功能详述
### F1. 触发范围
| 维度 | 规则 |
|------|------|
| 核心筛选 | 短剧绑定字段为空 |
| 上传时间窗口 | 上传时间 ∈ `[当前时间-7天, 当前时间-24h]`，仅处理最近一周内上传且已过 24h 手动保护期的素材 |
| 状态过滤 | 仅「已入库 / 稳定态」，排除上传中、待处理等非稳定记录 |
| 来源范围 | 满足上述条件的全部素材，不做来源过滤（如需来源黑名单排除位可扩展） |
| 批量处理 | 分批处理，每批串行调用推导服务，单轮上限可配置，默认 500 条 |
| 写回条件 | `UPDATE ... WHERE 绑定为空`，绝不覆盖已有绑定 |

明确边界：
- 历史存量未绑定素材（上传时间 > 7 天）不在本轮处理范围，存量过多，逐批清核另立方案。
- 已绑定的素材一律不动。
### F2. 定时时间
| 维度 | 规则 |
|------|------|
| 频率 | 每 6 小时一次（每日 4 轮），未绑定池量小，当天闭环足够 |
| 默认时刻 | `30 */6 * * *` → 00:30 / 06:30 / 12:30 / 18:30，服务器本地时区 |
| 错峰 | 基准刻偏移可调，避免与兄弟系统批处理窗口重叠，不涉跨日 / 时区换算 |
| 保护期 | 每条素材入库满 24h 后才纳入，给人工手动补绑窗口 |
| 重跑 | 全幂等，可手动重跑任意轮次，同一时间仅允许单实例运行 |
### F3. 处理流程
每 6h 触发后依次执行：
1. 单实例校验：已有运行中实例则本轮退出。
2. 查询未绑定池：绑定为空，且上传时间 ∈ `[now-7d, now-24h]`，且素材状态为稳定态 / 已入库。
3. 分批逐条调用「文件名 → 短剧」推导服务：推导成功则写回绑定（`WHERE 绑定为空`），推导失败则跳过。
4. 输出一行汇总日志：本轮池大小 / 成功数 / 跳过数。
### F4. 数据安全红线
- 写回一律带 `WHERE 绑定为空` 条件：与用户手动补绑产生竞态时写不到该行即跳过，绝不覆盖。
- 推导失败 / 无结果：不猜测、不写任何默认绑定值。
- 已绑定素材永不被本次任务触碰。
- 任务无状态：不建失败记账表，推导失败素材下轮自动重试。
### F5. 错误处理
| 场景 | 处理 |
|------|------|
| 单条推导失败 | 静默跳过，不影响本轮其余素材 |
| 推导服务整体不可用 / 超时 | 本轮整体跳过，不写任何绑定，触发告警，下轮自动重试 |
| 数据库异常 | 记录错误并中止本轮，下次窗口自动重试 |
| 叠加运行 | 单实例锁 + 幂等写入，双保险 |
### F6. 可观测性
- 每轮输出一行汇总日志：池大小 / 成功 / 跳过（跑量为零为正常态，不告警）。
- 仅系统性故障（推导服务不可用、写库失败）触发告警。
---
## 实现决策
1. **数据模型查空绑定**：以「绑定为空」为唯一取数入口，不引入来源链路，覆盖各生产路径。
2. **7 天窗口限定范围**：仅处理最近一周上传的未绑定素材，规避历史存量一次性错绑风险。
3. **24h 手动保护期**：给人工补绑留窗口，任务兜底保护期后、随 6h 节奏尽快补。
4. **静默跳过 + 无状态重试**：单条失败不记账、不告警，下轮自动重试，仅以汇总日志兜底可观测。
5. **写回防覆盖**：写语句强制 `WHERE 绑定为空`，并发手动补绑时不覆盖用户已保存绑定。
6. **全幂等 + 单实例**：重复 / 重叠触发无副作用，随时可手动重跑。
## 测试建议
- 任务每 6 小时自动触发一次，按 cron 可验证。
- 只处理绑定为空 + 上传时间最近 7 天 + 已过 24h 保护期 + 稳定态的素材，其余一律不碰。
- 上传时间超过 7 天的存量未绑定素材不被处理。
- 推导成功的素材绑定正确写回，写回带 `WHERE 绑定为空` 条件。
- 推导失败的素材保持未绑定、任务不崩溃、不写默认值，且下轮自动重试。
- 已有绑定的素材在任何情况下不被改动 / 覆盖。
- 与手动补绑并发时，不覆盖用户已保存的绑定。
- 每轮输出一行汇总日志（池大小 / 成功 / 跳过）。
## 不涉及
- 历史存量未绑定素材（上传时间 > 7 天）的批量清理，量过大，另立清核方案。
- 前端「无视风险继续上传」拦截策略的收紧或移除。
- 失败清单 / 工单系统，本任务按「静默跳过 + 汇总日志」设计。
- 绑定纠错（已有绑定纠错）与短剧数据本身的维护。