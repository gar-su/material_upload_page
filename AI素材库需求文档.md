# 需求文档：AI素材库（AI 创作平台素材同步中转）
## 版本
| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-09-09 | - | 初稿 |
## 背景
AI 创作平台（海外版）产出的素材需要进入 NS 批量素材库参与投放。原链路是 AI 平台素材直接同步到批量素材库，缺少中间环节，导致素材归属无法对应到人、同步后的命名无法批量规整、AI 平台侧与 NS 侧状态互相耦合。

本需求在 NS 后台新增「AI素材库」中转页：AI 创作平台素材先同步到 NS 的 AI素材库（按飞书用户体系归属到人），用户在 AI素材库中勾选素材、批量命名后，再绑定并同步到批量素材库的指定文件夹。
## 目标
- AI 创作平台海外版支持将素材同步至 NS 后台 AI素材库，素材归属到发起同步的飞书用户。
- NS 后台新增 AI素材库页面，仅展示当前用户自己同步过来的素材。
- AI素材库支持勾选素材、批量命名，并同步到 NS 批量素材库的指定文件夹。
- 同步成功后素材从 AI素材库中隐藏，数据按保留期清理。
## 范围
- **涉及页面**：NS 后台 - 素材库 - 新增「AI素材库」tab；AI 创作平台海外版 - AI 素材列表（新增同步按钮）
- **涉及系统**：AI 创作平台海外版（推送方）、NS 后台、NS 批量素材库素材桶
- **涉及功能**：素材接收与入库、AI素材库列表展示、批量命名、绑定目标文件夹并同步、数据生命周期管理
---
## 功能详述
### F1. 功能入口（AI 创作平台 → NS AI素材库）
| 维度 | 规则 |
|------|------|
| 入口位置 | AI 创作平台海外版 - AI 素材列表，新增「同步」按钮 |
| 触发方式 | 勾选素材后点击「同步」，发起素材同步 |
| 推送方向 | AI 创作平台 → NS 后台 AI素材库，单向 |
| 归属映射 | 按飞书用户体系映射，素材归属到发起同步的飞书用户 |
| 幂等键 | 待确认 Q2。同一幂等键重复推送按更新处理，不产生重复卡片、不重复入库 |
| 重复推送 | 待确认 Q1。无论 AI 平台是否允许重复推送，NS 侧均按幂等键去重，不依赖上游 |
| 推送回执 | NS 提供接收接口，返回成功/失败及失败原因；NS 不落推送任务状态 |
| 文件传输 | 待确认 Q3：AI 平台推送至 NS 素材桶 / NS 从 AI 平台拉取；失败重试次数 |
### F2. AI素材库页面
| 维度 | 规则 |
|------|------|
| 入口 | NS 后台 - 素材库 - 顶部 tab 新增「AI素材库」，与「批量素材库」平级 |
| 页面结构 | 无左侧素材树。自上而下：操作栏（同步 / 刷新）、素材名称搜索、列表信息（全选 / 已选数 / 总数）、素材卡片列表 |
| 搜索 | 输入素材名称后点「查询」触发，不做实时过滤；大小写不敏感 |
| 列表信息 | 展示「全选 / 已选 N 个素材 / 共 M 个素材」，M 随筛选结果变化 |
| 卡片字段 | 缩略图、时长、素材名（不带扩展名）、尺寸、来源、同步时间 |
| 素材名展示 | 统一不带扩展名，如 `895_QIKEYOU_EC_TR_P569_260909_FF_NU_Q`；名称超长时截断，悬停展示完整名称 |
| 状态展示 | 正常态不展示状态；同步失败时卡片展示失败标签与重试入口（待确认 Q15） |
| 默认排序 | 待确认 Q14，建议按同步时间倒序 |
| 分页 | 待确认 Q14，建议分页或懒加载 |
| 空态 | 待确认 Q15 |
### F3. 批量命名与同步到批量素材库
| 维度 | 规则 |
|------|------|
| 触发 | 勾选素材后点击「同步」，拉起「同步素材」弹窗 |
| 未勾选 | 提示「请先选择素材」，不打开弹窗 |
| 弹窗内容 | 已选素材数量、首个素材名称、同步至文件夹、命名预览、命名冲突提示 |
| 首个素材名称 | 默认预填第一个选中素材的原名（不含扩展名），可编辑 |
| 递增规则 | 自动识别名称中第一个数字段作为递增段，按选中顺序递增，如 895 → 896 / 897；保留原位数，有前导零则补零 |
| 命名预览 | 只展示首个素材一行「原名称 → 同步后名称」；名称超长时截断，悬停展示完整名称 |
| 同步后名称 | 等于首个素材名称按选中顺序递增后的结果，不带扩展名 |
| 目标文件夹 | 待确认 Q8：全部批量素材库文件夹 / 当前用户有权限的文件夹 |
| 命名冲突 | 已定：素材名在批量素材库内全库唯一，同步后名称与已有素材同名时禁止同步，提示调整首个素材名称，不跳过、不覆盖。查重按「同步后名称 + 源文件扩展名」比对，预览仍按不带扩展名展示（完整口径见 `重名素材拦截需求文档.md` F1、F3） |
| 同步结果 | 成功后卡片回显「已同步至 XXX 文件夹」；部分失败的处理方式待确认 Q7 |
| 单次上限 | 待确认 Q9 |
### F4. 数据生命周期
| 维度 | 规则 |
|------|------|
| 推送状态 | AI 平台 → AI素材库的推送，NS 不管理推送任务状态 |
| 同步成功后 | 素材在 AI素材库列表中隐藏，逻辑删除，不再展示 |
| 记录保留 | 逻辑删除的记录保留 30 天，到期物理删除。待确认 Q4：是否同时删除素材桶对象、口径以原文「只保留近一个月数据」为准的拆解方式 |
| 未同步素材 | 待确认 Q5：是否设保留期 |
| 清理任务 | 待确认 Q4：触发方、频率、是否单实例 |
| 溯源 | 待确认：逻辑删除期间是否保留「素材 → 批量素材库文件夹」的同步记录 |
### F5. 权限与用户映射
| 维度 | 规则 |
|------|------|
| 可见范围 | 该页面只展示当前用户自己同步过来的素材 |
| 用户映射 | AI 创作平台用户按飞书用户体系映射到 NS 账号 |
| 未映射用户 | 待确认 Q10：拒绝推送并返回原因 / 落到默认账号 |
| 管理员可见性 | 待确认 Q11：管理员、超管是否可见全部素材 |
| 一人多账号 | 待确认 Q10：同一人多个飞书账号是否合并 |
### F6. 数据字段与存储
| 维度 | 规则 |
|------|------|
| 存储位置 | 素材文件同步至 NS 批量素材库素材桶 |
| 保留字段 | 待确认 Q12。建议清单：素材 ID、文件名、尺寸、时长、大小、缩略图、来源项目、AI 平台创建时间、同步时间、同步人 |
| 格式限制 | 待确认 Q13：支持 mp4 / mov / 图片 |
| 大小限制 | 待确认 Q13：单文件上限与超限处理 |
---
## 交互流程
```
AI 创作平台海外版 - AI 素材列表
  → 勾选素材 → 点「同步」
  → 按飞书用户映射推送至 NS AI素材库（幂等键去重）
  → NS 后台 - 素材库 - AI素材库 tab
  → 勾选素材（可先用素材名称搜索）
  → 点「同步」→ 拉起「同步素材」弹窗
      ├─ 首个素材名称（默认预填首个选中素材原名，可编辑）
      ├─ 系统识别第一个数字段递增，实时刷新命名预览
      ├─ 选择「同步至」的批量素材库文件夹
      ├─ 生成名称与批量素材库已有素材同名时提示冲突，点「确定同步」被阻断
      └─ 调整首个素材名称消除冲突后点「确定同步」
  → 素材写入批量素材库指定文件夹，卡片回显「已同步至 XXX」
  → 该素材在 AI素材库中隐藏（逻辑删除）
```
---
## 验收标准
1. AI 创作平台勾选素材点「同步」，素材按飞书用户归属出现在 NS 的 AI素材库页面。
2. AI素材库页面只展示当前用户自己同步过来的素材。
3. 同一素材重复推送，AI素材库不产生重复卡片、不重复入库。
4. 页面无左侧素材树；搜索按「查询」按钮触发，不做实时过滤，大小写不敏感。
5. 列表信息中的素材总数随筛选结果变化。
6. 未勾选素材点「同步」提示「请先选择素材」，不打开弹窗。
7. 弹窗中首个素材名称默认预填首个选中素材原名，且不含扩展名。
8. 首个素材名称中第一个数字段按选中顺序递增（895 → 896 / 897），保留原位数，前导零补零。
9. 命名预览只展示一行，名称超长时悬停展示完整名称。
10. 确定同步后，素材出现在批量素材库指定文件夹，同时在 AI素材库中隐藏。
11. 同步成功的卡片回显「已同步至 XXX 文件夹」。
12. 同步失败时卡片展示失败状态并可重试（待 Q7、Q15 确认后细化）。
13. 生成名称与批量素材库已有素材同名时，弹窗内提示冲突且「确定同步」被阻断；调整首个素材名称消除冲突后可正常同步。
---
## 待确认项
| 编号 | 事项 | 影响 | 建议方案 |
|------|------|------|------|
| Q1 | AI 平台是否允许重复推送 | 决定去重实现是否可依赖上游 | NS 侧始终按幂等键去重，不依赖上游 |
| Q2 | 幂等键取值 | 决定重复素材的识别方式 | 优先用 AI 平台素材 ID，无则用素材 URL + 文件名 + 大小 |
| Q3 | 文件传输方式与失败重试 | 决定同步链路与失败处理 | 明确发起方、通道、重试次数 |
| Q4 | 「逻辑删除 + 保留一个月」的口径 | 决定清理任务与存储成本 | 拆为「列表隐藏 + 记录保留 30 天」，明确是否删桶内对象 |
| Q5 | 未同步素材是否设保留期 | 决定清理范围 | 待定 |
| Q6 | 命名冲突处理 | 决定是否覆盖已有素材 | 已定：全库同名即阻断同步，不跳过不覆盖，见 F3 |
| Q7 | 同步是同步接口还是异步任务 | 决定进度反馈与重试设计 | 素材体积大，建议异步 |
| Q8 | 目标文件夹可选范围 | 决定权限校验逻辑 | 当前用户有权限的文件夹 |
| Q9 | 单次同步数量上限 | 决定分批与超时策略 | 待定 |
| Q10 | 未映射飞书用户的处理 | 决定推送失败行为 | 拒绝推送并返回原因 |
| Q11 | 管理员是否可见全部素材 | 决定权限模型 | 待定 |
| Q12 | 保留字段清单 | 决定表结构 | 见 F6 建议清单 |
| Q13 | 格式与大小限制 | 决定校验与失败处理 | 待定 |
| Q14 | 分页与默认排序 | 决定页面性能与实现 | 同步时间倒序 + 分页 |
| Q15 | 空态文案与失败态展示 | 决定前端实现 | 待定 |
| Q16 | 非功能指标（数据量、性能、监控） | 决定容量与告警设计 | 待定 |
---
## Mock 数据说明
当前原型 `index.html` 中 AI素材库数据为前端写死的 mock 数据，接入真实接口时替换即可，交互逻辑无需改动：
| 数据 | 位置 | 说明 |
|------|------|------|
| AI 素材列表 | `#page-ai` 卡片网格 | 10 条静态卡片，`data-name` 保存原始文件名 |
| 同步目标文件夹 `MOCK_AI_FOLDERS` | `<script>` | 取自批量素材库文件夹，5 项 |
| 命名递增逻辑 | `findSeedSegment` / `buildSyncedName` | 取第一个数字段递增，保留位数 |
---
## 非功能
- 数据量预估：待确认 Q16，需明确日均同步素材数、平均单素材大小、近一个月存储增长。
- 页面性能：待确认 Q16，需明确单用户近一个月素材量下的加载策略。
- 监控告警：待确认 Q16，需明确同步成功率、失败率、素材桶写入失败的告警阈值。
---
## 不涉及（范围外）
- 第一期只涉及素材批量同步，不涉及同步任务的进度查询、重试、取消等管理能力。
- 素材同步到 NS 批量素材库素材桶，不涉及批量素材库其他功能改造。
- 平台之间数据不需要同步，删除等操作互不影响。
- 不涉及 AI 创作平台侧的产品改造，同步按钮之外的交互由 AI 平台侧定义。
