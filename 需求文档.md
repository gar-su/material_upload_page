# 上传素材页 - 短剧绑定、未绑定拦截与同名拦截需求文档
## 1. 项目背景
上传素材弹窗用于将图片/视频素材上传至指定目录。当前素材文件列表中的"短剧信息"列为待绑定状态，需支持为每个素材绑定一条短剧信息（剧本 + 语种 + 短剧三级关联），并在确认上传时拦截"未绑定短剧"的素材，防止无效素材入库。同时素材名在素材库内全局唯一，需在文件加入列表时拦截同名素材，避免同一条素材重复入库。
## 2. 需求一：选择短剧弹窗
### 2.1 触发方式
点击文件列表中"短剧信息"列的待绑定区域（初始文案为"待手动选择"）时，拉起"选择短剧"弹窗。
### 2.2 弹窗内容
- 标题：**选择短剧**
- 三个下拉选框，均为必填：
  1. **剧本信息**：列出全部剧本（编号 + 名称，如 `LA001-THE JANITOR: Rise of The Prime`）
  2. **语种**：列出全部支持语种（如 English / Spanish / Portuguese / French）
  3. **短剧**：初始禁用，展示提示"请先选择剧本信息和语种"
- 底部按钮：**取消**（关闭弹窗，不保存）、**确定**（校验通过后保存并回显）
### 2.3 联动规则
- 三个下拉均必填，未完整选择时点击"确定"给出提示，不关闭弹窗。
- 剧本信息、语种均选完后，**短剧下拉自动启用**，仅展示同时满足"所选剧本 + 所选语种"两个条件的短剧列表，并**自动默认填入第一条匹配短剧**（可手动改选）。
- 所选组合下无短剧时，短剧下拉保持禁用并提示"无匹配短剧，请调整剧本和语种"。
- 弹窗再次打开时，回填该素材上次已选的内容，便于修改。
### 2.4 确认后回显
确认后弹窗关闭，返回上传页面，"短剧信息"列显示**文本形式的返回结构**：
```
剧本编号-剧本名称-语种-短剧名称
```
示例（剧本编号为 mock 数据，如 LA001）：
```
LA001-THE JANITOR: Rise of The Prime-English-JANITOR: S1
```
- 该单元格由"待手动选择"变为已绑定文本，文案后附"✎ 重选"标识。
- 点击已绑定文本可**再次打开弹窗重新选择**，重新选择会覆盖原绑定。
- 已绑定状态即为该素材"绑定短剧"完成，参与上传时的完整性校验。
## 3. 需求二：未绑定短剧拦截
### 3.1 触发时机
点击上传页面底部"**完成**"按钮时，对文件列表中所有素材进行校验：是否存在未绑定短剧的素材。
### 3.2 校验结果
- **全部已绑定**：放行，进入正常上传流程。
- **存在未绑定**：拉起提示弹窗，标题为"提示"，文案为：
```
有 N 个素材未绑定短剧，确认无视风险继续上传？
```
（N 为未绑定素材数量，突出显示。）
### 3.3 提示弹窗选项
| 按钮 | 行为 |
| ---- | ---- |
| **查看** | 关闭提示弹窗，返回上传页面，供用户补齐绑定 |
| **确认** | 关闭提示弹窗，**无视风险继续上传**（放行） |
## 4. 需求三：同名素材拦截
### 4.1 判定规则
素材名在素材库内全局唯一，不限文件夹。同名判定口径：
| 维度 | 规则 |
| ---- | ---- |
| 比较对象 | 全库已有素材名 + 本次列表内已加入的其他素材名 |
| 名称构成 | 主名 + 扩展名，扩展名参与比较 |
| 规范化 | 去除首尾空格后忽略大小写 |
| 空主名 | 视为不可上传，与同名一并拦截 |
由此，`968_XXX.mp4` 与 `968_XXX.mov` 视为不同素材可共存，`968_XXX.mp4` 与 ` 968_xxx.MP4 ` 视为同名。
### 4.2 触发时机：加入列表时即时校验
文件加入列表时即查询全库并判定，不等点击"完成"。同名文件仍加入列表并就地标红，不做静默丢弃，便于用户直接改名。
### 4.3 名称编辑
文件列表"名称"列的主名可编辑，扩展名固定展示不可编辑，防止改名后名实不符。修改主名后实时重新判定，冲突解除即清除标红。
### 4.4 上传拦截
点击"完成"时先校验同名，再校验短剧绑定。存在同名或空主名时弹出提示弹窗，文案为：
```
有 N 个素材与素材库已有素材同名，请重命名后再上传
```
（N 为冲突素材数量，突出显示。）
该弹窗仅提供"知道了"关闭入口，不提供放行选项——同名素材禁止上传，与"未绑定短剧"可无视风险放行的语义相反，两者不可合并为同一弹窗。
### 4.5 与 AI素材库同步的关系
全库唯一键对 AI素材库同步入口同样生效，完整口径（两个入口的判定、后端接口与验收标准）见 `重名素材拦截需求文档.md`。
## 5. Mock 数据说明
当前阶段所有数据均为**前端写死的 mock 数据**，集中定义在 `index.html` 的 `<script>` 内，便于后续替换为真实接口：
| 数据 | 结构 | 示例 |
| ---- | ---- | ---- |
| 剧本信息 `MOCK_PLAYS` | `{ id, name }` | `{ id: 'LA001', name: 'THE JANITOR: Rise of The Prime' }` |
| 语种 `MOCK_LANGUAGES` | 字符串数组 | `'English', 'Spanish', 'Portuguese', 'French'` |
| 短剧 `MOCK_DRAMAS` | `key = 剧本ID_语种`，值为短剧名数组 | `'LA001_English': ['JANITOR: S1', ...]` |
| 全库已有素材名 `MOCK_LIBRARY_NAMES` | 字符串数组，含扩展名 | `'1204_QIKEYOU_EC_TR_P560_260901_FF_NU_Q.mp4'` |
| 模拟加入列表的文件 `MOCK_UPLOAD_FILES` | `{ name, dim, size }` | `{ name: '..._Q.mp4', dim: '1080 * 1920', size: '88MB' }` |
返回结构中的"剧本编号"即 `MOCK_PLAYS[].id`（mock 如 `LA001`）。`MOCK_UPLOAD_FILES` 用于模拟"加入列表"动作，点击拖拽区或上传按钮逐个追加，其中前两条与 `MOCK_LIBRARY_NAMES` 同名、第三条仅扩展名不同，用于演示同名拦截的边界。
## 6. 交互流程
### 6.1 绑定短剧
```
点击"待手动选择" → 打开选择短剧弹窗
  → 选择剧本信息 + 语种 → 短剧下拉自动加载匹配列表并默认填入第一条
  → （可改选短剧）→ 点击确定（未选全则提示不关闭）
  → 关闭弹窗，单元格回显「剧本编号-剧本名称-语种-短剧名称」
  → 点击已绑定文本可重选（覆盖原绑定）
```
### 6.2 加入文件与同名校验
```
点击拖拽区 / 文件夹上传 / 上传素材 → 逐个加入文件行
  → 即时按「全库已有素材名 + 本批内其他行」判定
      ├─ 无冲突 → 正常展示
      └─ 同名或空主名 → 行内标红并提示原因，仍保留在列表中
  → 用户就地修改主名（扩展名固定不可改）→ 实时重新判定
```
### 6.3 确认上传
```
点击"完成" → 先校验同名，再校验短剧绑定
  ├─ 存在同名或空主名 → 弹出"有 N 个素材与素材库已有素材同名，请重命名后再上传"
  │     └─ 知道了 → 关闭弹窗，返回上传页改名（无放行入口）
  └─ 无同名 → 遍历所有素材检查绑定状态
        ├─ 全部已绑定 → 进入上传流程
        └─ 存在未绑定 → 弹出"有 N 个素材未绑定短剧，确认无视风险继续上传？"
              ├─ 查看 → 关闭弹窗，返回上传页补绑
              └─ 确认 → 关闭弹窗，无视风险继续上传
```
## 7. 验收标准
1. 点击"待手动选择"能打开"选择短剧"弹窗，标题正确。
2. 弹窗内剧本信息、语种、短剧三项均必填，未选全点"确定"被拦截并提示。
3. 仅当剧本信息、语种都选中后，短剧下拉才启用，列表只含匹配该剧本 + 语种的短剧，且**自动默认填入第一条匹配短剧**，用户可直接改选。
4. 确认后返回上传页，单元格显示 `剧本编号-剧本名称-语种-短剧名称` 结构文本（剧本编号 mock 如 `LA001`）。
5. 点击已绑定文本可重新打开弹窗并重选，覆盖原绑定。
6. 素材未绑定短剧时点"完成"，弹出"有素材未绑定短剧"提示，并显示未绑定数量。
7. 提示弹窗点"查看"返回上传页面，不触发上传；点"确认"无视风险继续上传。
8. 素材全部已绑定时点"完成"，直接进入上传流程，不弹出拦截提示。
9. 文件加入列表时即与全库已有素材名比对，同名的行标红并提示"与素材库已有素材同名"，且仍保留在列表中不被丢弃。
10. 同名判定忽略大小写与首尾空格，扩展名参与比较：`968_XXX.mp4` 与 `968_XXX.mov` 不判为同名。
11. 本次列表内两行同名时同样标红。
12. 名称列主名可编辑、扩展名固定不可编辑；改名后实时重新判定，冲突解除即清除标红。
13. 存在同名或空主名时点"完成"，弹出同名拦截弹窗并显示冲突数量，弹窗不提供放行入口；冲突清除后点"完成"回到原有的未绑定短剧拦截逻辑。
## 8. 后续接入真实接口说明
接入真实后端时，只需将 `<script>` 中 `MOCK_PLAYS` / `MOCK_LANGUAGES` / `MOCK_DRAMAS` 三处替换为接口返回值：
- 剧本信息：`GET /plays` → `[{ id, name }]`
- 语种：`GET /languages` → `['English', ...]`
- 短剧（按剧本 + 语种过滤）：`GET /dramas?playId={id}&lang={lang}` → `[短剧名]`
同名拦截需替换 `MOCK_LIBRARY_NAMES` 与 `MOCK_UPLOAD_FILES`，并按批设计查名接口——「加入即查全库」若逐个文件调用，批量拖入 50 个文件即产生 50 次请求，建议按批提交：
- 批量查名：`POST /materials/check-names`，请求体 `{ names: ['968_XXX.mp4', ...] }` → `{ conflicts: ['968_XXX.mp4', ...] }`
前端将名称规范化后成批提交，按返回的冲突名单标红对应行。
联动逻辑（`refreshDramaOptions`）、绑定回显与上传拦截逻辑无需改动。
## 9. 范围外（本期不做）
- 文件列表的删除、真实拖拽与选择文件的读取（本期仅以 `MOCK_UPLOAD_FILES` 模拟"加入列表"，不做真实文件读取）。
- 上传进度展示与真实上传。
- 短剧信息与素材的真实持久化存储。
