# 上传素材页 - 短剧绑定与未绑定拦截需求文档

## 1. 项目背景

上传素材弹窗用于将图片/视频素材上传至指定目录。当前素材文件列表中的"短剧信息"列为待绑定状态，需支持为每个素材绑定一条短剧信息（剧本 + 语种 + 短剧三级关联），并在确认上传时拦截"未绑定短剧"的素材，防止无效素材入库。

## 2. 需求一：选择短剧弹窗

### 2.1 触发方式

点击文件列表中"短剧信息"列的待绑定区域（初始文案为"待手动选择"）时，拉起"选择短剧"弹窗。

### 2.2 弹窗内容

- 标题：**选择短剧**
- 三个下拉选框，均为必填：
  1. **剧本信息**：列出全部剧本（编号 + 名称，如 `LA001-THE JANITOR: Rise of The Prime`）
  2. **语种**：列出全部支持语种（如 English / Spanish / Portuguese / French）
  3. **短剧**：初始禁用，展示提示"请先选择剧本信息和语种"
- 底部按钮：**取消**（关闭弹窗，不保存）、**确定**（校验通过后保存并回显）

### 2.3 联动规则

- 三个下拉均必填，未完整选择时点击"确定"给出提示，不关闭弹窗。
- 剧本信息、语种均选完后，**短剧下拉自动启用**，仅展示同时满足"所选剧本 + 所选语种"两个条件的短剧列表，并**自动默认填入第一条匹配短剧**（可手动改选）。
- 所选组合下无短剧时，短剧下拉保持禁用并提示"无匹配短剧，请调整剧本和语种"。
- 弹窗再次打开时，回填该素材上次已选的内容，便于修改。

### 2.4 确认后回显

确认后弹窗关闭，返回上传页面，"短剧信息"列显示**文本形式的返回结构**：

```
剧本编号-剧本名称-语种-短剧名称
```

示例（剧本编号为 mock 数据，如 LA001）：

```
LA001-THE JANITOR: Rise of The Prime-English-JANITOR: S1
```

- 该单元格由"待手动选择"变为已绑定文本，文案后附"✎ 重选"标识。
- 点击已绑定文本可**再次打开弹窗重新选择**，重新选择会覆盖原绑定。
- 已绑定状态即为该素材"绑定短剧"完成，参与上传时的完整性校验。

## 3. 需求二：未绑定短剧拦截

### 3.1 触发时机

点击上传页面底部"**完成**"按钮时，对文件列表中所有素材进行校验：是否存在未绑定短剧的素材。

### 3.2 校验结果

- **全部已绑定**：放行，进入正常上传流程。
- **存在未绑定**：拉起提示弹窗，标题为"提示"，文案为：

```
有 N 个素材未绑定短剧，确认无视风险继续上传？
```

（N 为未绑定素材数量，突出显示。）

### 3.3 提示弹窗选项

| 按钮 | 行为 |
| ---- | ---- |
| **查看** | 关闭提示弹窗，返回上传页面，供用户补齐绑定 |
| **确认** | 关闭提示弹窗，**无视风险继续上传**（放行） |

## 4. Mock 数据说明

当前阶段所有数据均为**前端写死的 mock 数据**，集中定义在 `index.html` 的 `<script>` 内，便于后续替换为真实接口：

| 数据 | 结构 | 示例 |
| ---- | ---- | ---- |
| 剧本信息 `MOCK_PLAYS` | `{ id, name }` | `{ id: 'LA001', name: 'THE JANITOR: Rise of The Prime' }` |
| 语种 `MOCK_LANGUAGES` | 字符串数组 | `'English', 'Spanish', 'Portuguese', 'French'` |
| 短剧 `MOCK_DRAMAS` | `key = 剧本ID_语种`，值为短剧名数组 | `'LA001_English': ['JANITOR: S1', ...]` |

返回结构中的"剧本编号"即 `MOCK_PLAYS[].id`（mock 如 `LA001`）。

## 5. 交互流程

### 5.1 绑定短剧

```
点击"待手动选择" → 打开选择短剧弹窗
  → 选择剧本信息 + 语种 → 短剧下拉自动加载匹配列表并默认填入第一条
  → （可改选短剧）→ 点击确定（未选全则提示不关闭）
  → 关闭弹窗，单元格回显「剧本编号-剧本名称-语种-短剧名称」
  → 点击已绑定文本可重选（覆盖原绑定）
```

### 5.2 确认上传

```
点击"完成" → 遍历所有素材检查绑定状态
  ├─ 全部已绑定 → 进入上传流程
  └─ 存在未绑定 → 弹出"有 N 个素材未绑定短剧，确认无视风险继续上传？"
        ├─ 查看 → 关闭弹窗，返回上传页补绑
        └─ 确认 → 关闭弹窗，无视风险继续上传
```

## 6. 验收标准

1. 点击"待手动选择"能打开"选择短剧"弹窗，标题正确。
2. 弹窗内剧本信息、语种、短剧三项均必填，未选全点"确定"被拦截并提示。
3. 仅当剧本信息、语种都选中后，短剧下拉才启用，列表只含匹配该剧本 + 语种的短剧，且**自动默认填入第一条匹配短剧**，用户可直接改选。
4. 确认后返回上传页，单元格显示 `剧本编号-剧本名称-语种-短剧名称` 结构文本（剧本编号 mock 如 `LA001`）。
5. 点击已绑定文本可重新打开弹窗并重选，覆盖原绑定。
6. 素材未绑定短剧时点"完成"，弹出"有素材未绑定短剧"提示，并显示未绑定数量。
7. 提示弹窗点"查看"返回上传页面，不触发上传；点"确认"无视风险继续上传。
8. 素材全部已绑定时点"完成"，直接进入上传流程，不弹出拦截提示。

## 7. 后续接入真实接口说明

接入真实后端时，只需将 `<script>` 中 `MOCK_PLAYS` / `MOCK_LANGUAGES` / `MOCK_DRAMAS` 三处替换为接口返回值：

- 剧本信息：`GET /plays` → `[{ id, name }]`
- 语种：`GET /languages` → `['English', ...]`
- 短剧（按剧本 + 语种过滤）：`GET /dramas?playId={id}&lang={lang}` → `[短剧名]`

联动逻辑（`refreshDramaOptions`）、绑定回显与上传拦截逻辑无需改动。

## 8. 范围外（本期不做）

- 文件列表的动态增删（当前为静态单行示例）。
- 拖拽上传 / 文件夹上传的真实文件读取与上传进度。
- 短剧信息与素材的真实持久化存储。
